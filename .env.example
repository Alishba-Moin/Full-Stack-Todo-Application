# Root Environment Variables for Docker Compose
# Copy to /.env and fill in actual values

# Shared Authentication Secret (used by both frontend and backend)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
BETTER_AUTH_SECRET=your-super-secret-key-min-32-chars-random-string

# Note: Database credentials for Docker are in docker-compose.yml
# PostgreSQL in Docker uses: todo_user / todo_pass_dev_only / todo_dev