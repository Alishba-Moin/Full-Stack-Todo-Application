# Backend Environment Variables
# Copy to /backend/.env.development (for Docker) or /backend/.env.production (for Neon)

# === LOCAL DEVELOPMENT (Docker Compose) ===
# Use this for: docker-compose up
DATABASE_URL=postgresql://todo_user:todo_pass_dev_only@db:5432/todo_dev

# === PRODUCTION (Neon DB) ===
# Use this for: Staging/Production deployment
# DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require

# Authentication Secret (MUST match frontend!)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
BETTER_AUTH_SECRET=your-super-secret-key-min-32-chars-random-string

# JWT Configuration
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=7

# Environment
ENVIRONMENT=development

# Optional: Port configuration
PORT=8000
HOST=0.0.0.0